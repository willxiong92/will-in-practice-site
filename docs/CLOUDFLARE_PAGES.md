# Cloudflare Pages

现有站点 https://will-in-practice.pages.dev 保留。旧 GitHub 仓库删除后，其 Git 自动部署来源失效，新仓库尚未连接。

重新连接需另行授权；届时使用 `willxiong92/will-in-practice-site` 的 `main`，构建命令 `npm run build`，产物 `dist`，Node.js 22。不要将当前文档当作已完成连接的证据。

原有账号配置、Secrets 和 KV 均未清理。
